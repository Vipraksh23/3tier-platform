terraform {
  backend "s3" {

    bucket = "company-terraform-state"  # update bucket name as per account

    key = "dev/security/terraform.tfstate"

    region = "ap-south-1"

    encrypt = true

    dynamodb_table = "terraform-lock"

  }
}