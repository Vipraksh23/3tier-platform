# Backend configuration is intentionally disabled.
# Update the bucket, key, and DynamoDB table according to the target AWS account before running `terraform init -reconfigure`.


/*
terraform {
  backend "s3" {
    bucket         = "company-terraform-state"
    key            = "dev/shared/terraform.tfstate"
    region         = "ap-south-1"
    encrypt        = true
    dynamodb_table = "terraform-lock"
  }
}
*/