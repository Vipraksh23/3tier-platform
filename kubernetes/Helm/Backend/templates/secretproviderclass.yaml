apiVersion: secrets-store.csi.x-k8s.io/v1

kind: SecretProviderClass

metadata:
  name: {{ .Values.secretProvider.name }}

spec:

  provider: aws

  parameters:

    objects: |

      - objectName: "db-password"    # Based on secret configured

        objectType: "secretsmanager"